# Use the SAME two values as your school management system project — this website
# reads from the same Supabase database, just never writes to it.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
